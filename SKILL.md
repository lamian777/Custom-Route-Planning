# Codex 工作规则

预览地址固定：

http://127.0.0.1:8010/index.html

仅在启动预览时，为减少时间和 TOKEN 消耗：

- 直接使用固定预览地址，不重复搜索入口文件；
- 不重复启动或重启已经运行的预览服务；
- 用户只要求启动预览时，不额外做页面可访问性检查或功能测试。

修改完成后，必须复用已经打开的预览页面进行实际检查；确认页面加载、相关交互和控制台状态正常后，再通知用户。
